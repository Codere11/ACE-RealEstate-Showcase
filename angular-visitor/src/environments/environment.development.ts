export const environment = {
  production: false,
  apiUrl: '',  // proxied via proxy.conf.json
  tenantSlug: 'demo',
  pollingIntervalMs: 5000,
  pollingTimeout: 20,
  retryCount: 3,
  retryDelayMs: 1000,
};
